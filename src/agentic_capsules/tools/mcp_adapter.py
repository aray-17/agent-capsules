# MCP Adapter — abstracts the Model Context Protocol; insulates framework from protocol changes.
# Handles: server discovery, connection lifecycle, tool invocation, result deserialization.
# See design plan §5.1 (Tool Orchestrator row), risk analysis §8 (MCP protocol changes)

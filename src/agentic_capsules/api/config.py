# YAML config parser — loads pipeline and threshold configuration from YAML files.
# Supports: hierarchy definition, threshold overrides, model endpoint assignment,
#           tool chain declarations, and iteration space dimension specs.
# See design plan §5.1 (Capsule definitions row)
